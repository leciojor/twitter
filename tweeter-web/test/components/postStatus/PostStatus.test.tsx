import {
  mock,
  instance,
  spy,
  verify,
  anything,
  when,
} from "@typestrong/ts-mockito";
import PostStatus from "../../../src/components/postStatus/PostStatus";

describe("PostStatus", () => {
  it("When first rendered the Post Status and Clear buttons are both disabled", () => {});

  it("Both buttons are enabled when the text field has text", () => {});

  it("Both buttons are disabled when the text field is cleared", () => {});

  it("The presenter's postStatus method is called with correct parameters when the Post Status button is pressed", () => {});
});
